"use client";

import { ArrowLeft, ArrowRight, Check, Home, Plus, ReceiptText, Users, X } from "lucide-react";
import Link from "next/link";
import { useState } from "react";

const members = [
  { name: "Alex Morgan", email: "alex@maple.house", initials: "AM", color: "peach" },
  { name: "Kiran Patel", email: "kiran@maple.house", initials: "KP", color: "mint" },
  { name: "Sam Lee", email: "sam@maple.house", initials: "SL", color: "blue" },
  { name: "Jordan Rivera", email: "jordan@maple.house", initials: "JR", color: "violet" },
];

export default function OnboardingPage() {
  const [step, setStep] = useState(2);
  return <main className="setup-shell"><header className="setup-header"><Link href="/" className="setup-brand"><span className="brand-mark"><span className="roof" /><span className="door" /></span>FairShare</Link><span>Household setup</span><button><X size={18} /></button></header><div className="setup-layout"><aside><p>SET UP MAPLE HOUSE</p>{["Household details", "Add members", "Recurring bills", "Review & finish"].map((label, index) => <button key={label} className={step === index + 1 ? "active" : step > index + 1 ? "done" : ""} onClick={() => setStep(index + 1)}><span>{step > index + 1 ? <Check size={14} /> : index + 1}</span><strong>{label}</strong></button>)}</aside><section className="setup-card">{step === 1 && <><span className="setup-icon"><Home size={23} /></span><p className="eyebrow">STEP 1 OF 4</p><h1>Name your Household</h1><p>This is the shared space your members will see after signing in.</p><label>Household name<input defaultValue="Maple House" /></label><div className="setup-pair"><label>Currency<select defaultValue="USD"><option>USD — US Dollar</option></select></label><label>Timezone<select defaultValue="ET"><option value="ET">Eastern Time</option></select></label></div></>}{step === 2 && <><span className="setup-icon"><Users size={23} /></span><p className="eyebrow">STEP 2 OF 4</p><h1>Who lives here?</h1><p>Add existing FairShare users. An administrator controls final membership.</p><div className="member-list">{members.map((member) => <div key={member.email}><span className={`avatar ${member.color}`}>{member.initials}</span><span><strong>{member.name}</strong><small>{member.email}</small></span><span className="member-role">Member</span><button><X size={16} /></button></div>)}</div><button className="add-member"><Plus size={17} /> Add another member</button><div className="setup-info"><Users size={17} /><span><strong>4 Household members</strong><small>Each member will receive access after an administrator approves membership.</small></span></div></>}{step === 3 && <><span className="setup-icon"><ReceiptText size={23} /></span><p className="eyebrow">STEP 3 OF 4</p><h1>Add recurring bills</h1><p>Set these up once. FairShare will create independent bill instances on schedule.</p><div className="recurring-form"><label>Name<input defaultValue="Rent" /></label><label>Expected amount<input defaultValue="$2,400.00" /></label><label>Repeats<select><option>Monthly</option></select></label><label>Next bill<input type="date" defaultValue="2026-08-01" /></label></div><div className="recurring-row"><span className="activity-icon teal"><ReceiptText size={17} /></span><span><strong>Internet</strong><small>Monthly on the 18th · about $92</small></span><button>Edit</button></div><button className="add-member"><Plus size={17} /> Add another recurring bill</button></>}{step === 4 && <><span className="setup-icon"><Check size={23} /></span><p className="eyebrow">STEP 4 OF 4</p><h1>Maple House is ready</h1><p>Review the details below. You can update them later in Household settings.</p><div className="review-grid"><div><small>HOUSEHOLD</small><strong>Maple House</strong><span>USD · Eastern Time</span></div><div><small>MEMBERS</small><strong>4 people</strong><span>Pending admin approval</span></div><div><small>RECURRING BILLS</small><strong>2 schedules</strong><span>Rent and Internet</span></div></div></>}<footer><button className="secondary-button" onClick={() => step === 1 ? location.href = "/" : setStep(step - 1)}><ArrowLeft size={16} /> Back</button>{step < 4 ? <button className="primary-button" onClick={() => setStep(step + 1)}>Continue <ArrowRight size={16} /></button> : <Link href="/" className="primary-button">Enter Maple House <ArrowRight size={16} /></Link>}</footer></section></div></main>;
}
